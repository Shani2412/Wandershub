<% layout('/layouts/boilerplate') %>

<div class="row mt-3">
  <div class="col-8 offset-2">
    <form
      action="/listings/<%= listing._id %>?_method=PUT"
      method="POST"
      enctype="multipart/form-data"
      novalidate
      class="needs-validation"
    >
      <h2>Edit your List:</h2>

      <div class="mb-3">
        <label class="form-label">Enter Title</label>
        <input
          type="text"
          name="listing[title]"
          class="form-control"
          value="<%= listing.title %>"
          required
        />
        <div class="invalid-feedback">Please enter valid title</div>
      </div>

      <div class="mb-3">
        <label class="form-label">Enter Description</label>
        <input
          type="text"
          name="listing[description]"
          class="form-control"
          value="<%= listing.description %>"
          required
        />
        <div class="invalid-feedback">Please enter valid description</div>
      </div>

      <!-- 🔴 IMAGE FIELD UPDATED FOR CLOUDINARY -->
      <div class="mb-3">
        <label class="form-label">Update Image</label>
        <input
          type="file"
          name="image"
          class="form-control"
          accept="image/*"
        />
        <small class="text-muted">
          Leave empty to keep existing image
        </small>
      </div>

      <!-- 🔴 CURRENT IMAGE PREVIEW (OPTIONAL BUT GOOD UX) -->
      <% if (listing.image && listing.image.url) { %>
        <div class="mb-3">
          <img
            src="<%= listing.image.url %>"
            class="img-fluid rounded"
            style="max-height: 200px;"
          />
        </div>
      <% } %>

      <div class="row">
        <div class="mb-3 col-md-4">
          <label class="form-label">Enter Price</label>
          <input
            type="number"
            name="listing[price]"
            class="form-control"
            value="<%= listing.price %>"
            required
          />
          <div class="invalid-feedback">Please enter valid price</div>
        </div>

        <div class="mb-3 col-md-8">
          <label class="form-label">Enter Location</label>
          <input
            type="text"
            name="listing[location]"
            class="form-control"
            value="<%= listing.location %>"
            required
          />
          <div class="invalid-feedback">Please enter valid location</div>
        </div>
      </div>

      <div class="mb-3">
        <label class="form-label">Enter Country</label>
        <input
          type="text"
          name="listing[country]"
          class="form-control"
          value="<%= listing.country %>"
          required
        />
        <div class="invalid-feedback">Please enter valid country</div>
      </div>

      <button type="submit" class="btn btn-dark edit-btn mt-3 mb-3">
        Done
      </button>
    </form>
  </div>
</div>
